import {SafeAreaProvider} from 'react-native-safe-area-context';
import {Text} from './components';

const App = () => (
  <SafeAreaProvider>
    <Text>tuyen</Text>
  </SafeAreaProvider>
);

export default App;
